package omnivoxel.server.entity;

import omnivoxel.server.client.ServerItem;

public class ServerEntityTexture implements ServerItem {
    @Override
    public byte[] getBytes() {
        return new byte[0];
    }
}